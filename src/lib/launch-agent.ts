import { generateAgentWallet } from './solana-client'; import { logAgentAction } from './agent-core'; import { getLaunchPayload } from '../app/token-config'; export async function initializeLaunchAgent() { const agentWallet = generateAgentWallet(); await logAgentAction(agentWallet.publicKey, 'AGENT_INITIALIZED', { status: 'ready', protocol: 'pump.fun USDC' }); return { agentId: agentWallet.publicKey, payload: getLaunchPayload() }; }
