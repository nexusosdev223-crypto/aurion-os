import { Connection, Keypair } from '@solana/web3.js'; import { createClient } from '@supabase/supabase-js'; import WebSocket from 'ws'; globalThis.WebSocket = WebSocket; const supabase = createClient('http://127.0.0.1:54321', 'sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH'); async function runMasterSequence() { console.log('🧠 1. Connecting to Local AI Brain (Ollama/Qwen)...'); let aiLore = 'Forged by the Aurion OS agent network.'; try { const response = await fetch('http://127.0.0.1:11434/api/generate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ model: "tinyllama", prompt: 'Write one short, mysterious sentence describing the origin of a new digital token called Aurion Omega.', stream: false }) }); const aiData = await response.json(); if (aiData && aiData.response) { aiLore = aiData.response.trim(); console.log('💡 AI Generated Lore:', aiLore); } else { console.log('⚠️ AI Response was empty, payload:', JSON.stringify(aiData)); } } catch (error) { console.log('⚠️ AI Connection failed:', error.message); } console.log('\n⚡ 2. Establishing Solana Devnet Uplink...'); const agentWallet = Keypair.generate(); console.log('\🔑 Generated Devnet Wallet:', agentWallet.publicKey.toString()); console.log('\n💾 3. Broadcasting to Local UI Dashboard...'); await supabase.from('agent_logs').insert([{ agent_id: agentWallet.publicKey.toString(), action: 'DEVNET_FUNDED_AND_READY', status: 'success', memory_payload: { token: 'Ω', lore: aiLore, network: 'devnet' } }]); console.log('✅ Master Sequence Complete.'); process.exit(0); } runMasterSequence();
